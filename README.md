Writen on Arrivy for @[MaddyPrograms](https://github.com/MaddyPrograms)

Below is the explaination in Urdu/English

Api etc won't work anymore

# Dependencies:

Dependencies ka matlab hota hai kisi software, application ya code mein woh sab cheezain jin ki zaroorat hoti hai, jis se koi bhi program kaam karta hai wo cheezein dependencies kehlata hai.

For example, agar hum ek web application bana rahe hain tou usmein Javascript ka use karne ki zaroorat hogi, lekin hum pura Javascript code khud se likhenge tou ye bohat mushkil hoga. Isliye, hum npm jaise tools ka use karte hain jo humein pre-written libraries provide karte hain jinhe hum apni web application mein use kar sakte hain.

Jab hum in libraries ko use karte hain tou woh hamari web application ke liye dependencies ban jaate hain. Agar hum kisi aur code ke sath apna code share karna chahte hain tou dependencies uski performance aur compatibility ke liye bohat important hote hain, kyun ke dependencies humari web application ko woh functions provide karte hain jo use karni hoti hain.

Dependencies ko use karne se code likhne mein bhi bohat asaani hoti hai kyun ke hum pehle se code ko nahi likhna chahte hain, balki kisi aur ne usko pehle se hi likh liya hai aur hum use apni application mein use karna chahte hain. Isliye, dependencies code likhne mein time save karte hain aur uski quality bhi improve hoti hai kyun ke dependencies experienced developers ke dwara develop kiye jaate hain.

In short, dependencies ek important concept hain programming mein jis se hum code likhne mein time save karte hain aur better quality ka code likh sakte hain.

=========================

- [Python File](run.py):

=========================

About Pip:

Pip install karne ka matlab hai ke ham computer par kisi bhi Python library ya package ko download kar rahe hain. Jab ham Python ka koi program likhte hain aur usme koi third-party library ka use karna chahte hain, jese numpy, pandas ya matplotlib, to hum pip ka istemaal karte hain taake wo library download ho sake.

Pip Python ka package manager hai jo Python ke liye third-party libraries ko manage karta hai. Pip, jis tarah se aap ka computer kisi aur software ko download karne ke liye internet se files ko download karta hai, bilkul usi tarah se Python libraries ko download karta hai. Python ke bahut se libraries open source hote hain, jinhe Python community ne develop kiya hota hai. Yeh libraries hamein kuch pre-written code provide karte hain jise ham apne Python programs mein integrate kar sakte hain aur hamen apne program ko code ko likhne mein time aur effort bachate hain.

Pip ke zariye libraries download karne ke liye, hamein pip ka use karna hota hai. Pip se libraries ko download karne ka process kaafi easy hai, hum sirf ek command line mein pip install likh kar aur uske baad package ka naam likh kar, kisi bhi library ko install kar sakte hain. Pip ka use kar ke, hum bahut se third-party libraries aur packages apne Python projects mein add kar sakte hain aur hamari coding experience ko improve kar sakte hain.

How to install pip:

Pip ek aisa software hai jo Python ke packages ko install karne mein madad karta hai. Agar aapke paas Python hai aur aapko koi package install karna hai, toh pip ka upyog karna bahut aasan hai.

Pip ko install karna bahut hi aasan hai. Sabse pahle, aapko Python ke sath-sath pip ko bhi download karna hoga. Agar aapke paas Python nahi hai toh pehle aapko Python download karna hoga.

Pip ko install karne ke liye, aapko command prompt ya terminal open karna hoga aur fir "python get-pip.py" command ko run karna hoga. Iske baad, aap pip ke saath-saath koi bhi package install kar sakte hain.

Agar aap pip install karne ke baad koi error aata hai, toh aapko pip ko uninstall karke fir se install karna chahiye. Agar pip install karne ke baad pip sahi se kaam nahi kar raha hai, toh aapko apne system ki environment variables check karni hogi. Environment variables mein pip ka path sahi set hona chahiye.

=========================

Dependencies:

        Pip install requests
        Pip install csv
        Pip install os

Inside PY:

            import requests
            import csv
            import os

Explanation:

Jab hum kisi programming language jaise Python mein code likhtay hain aur kisi external module ya library ka use karna chahtay hain, to humein us library ko apnay computer par install karna hota hai. Iss installation ke liye Python mein "pip" naam ka tool hota hai.

For example, agar hum requests library ka use karna chahtay hain, to hum pip install requests command ko terminal mein type karenge aur phir hum us library ko apnay code mein "import requests" line se use kar sakte hain.

Isi tarah, jab hum CSV files ke saath kaam karte hain to hum CSV library ko use karte hain, jo Python mein by default available nahi hoti. Isliye hum pip install csv command ko use karke CSV library ko install karte hain aur phir usko apnay code mein "import csv" line se use karte hain.

Agar hum computer par file ya folder kaam kar rahe hain to hum os library ko use karte hain. Lekin yeh library bhi Python mein by default available nahi hoti, isliye hum pip install os command ko use karke os library ko install karte hain aur phir usko apnay code mein "import os" line se use karte hain.

Iss tarah se jab hum koi external library ya module ka use karte hain to humein pehle usko apnay computer par install karna hota hai, phir hi hum usko apnay code mein import kar sakte hain.

Note:

Main ne neeche diye gaye dependencies istemaal karne ki koshish ki thi lekin unke kaam na karne ki wajah se main unhein kat diya hai. Main ne unhein neeche shamil kar diya hai taake tum logon ko bata sako ke tumne bhi un methods ko try kiya tha, lekin unke kaam na karne ki koi wajah thi jo mujhe pata nahi.

<s>pip install flask</s>

<s>pip install flask-csv</s>

<s>pip install autopep8:</s>

ye neechay di gayei command autopop8 ki hai

<s>-    autopep8 --in-place --aggressive --aggressive run.py </s>

<s>pip install beautifulsoup4</s>

=========================

Details

Ye code Python mein likha gaya hai, jo keh programming language hai jis se web applications, data analysis, automation aur aur bohot kuch create kia jasakta hai. Is code mein kuch modules ya libraries use kia gaya hai jo keh extra functionality provide karte hain, jese keh requests, csv aur os. Ye modules code ke shuru mein import keyword se import karna zaroori hai.

Is code mein koshish ki gai hai keh kuch customer data ek API se lein, jo keh ek tarika hai dusre web service ya application se communicate karne ka. Ye API Arrivy ke taraf se provide ki jati hai, jo keh ek platform hai jis se businesses apne deliveries aur services manage kar sakte hain. API use karne ke liye, code ko ek API key aur ek API token ki zaroorat hai, jo keh unique identifiers hote hain jin se API access ho sakta hai. Code environment variables se ye values le raha hai, jo keh code ke bahir set kiye jate hain, os.environ.get method se. Code headers ko set kar raha hai, jo keh additional information hote hain jo API request ke sath send kiye jate hain, headers dictionary use kar ke.

Code requests module ka use kar raha hai HTTP requests ko API mein bhejne ke liye, jo keh internet se data send aur receive karne ka tarika hai. Code get method use kar raha hai API se data lene ke liye aur URL aur headers ko arguments ke tor pe pass kar raha hai. URL API endpoint ka address hai, jo keh API provide karta hai ek specific function ya resource ke tor pe. Code base_url variable ko use kar ke URL ka common part store kar raha hai, jo keh https://app.arrivy.com/api hai.

Code pehle /customers endpoint ko request bhej raha hai, jo keh Arrivy ke sath registered customers ki list return karta hai. Code response ka status code check kar raha hai, jo keh ek number hai jo indicate karta hai keh request successful hua hai ya nahi. Agar status code 200 hai, jo keh OK hai, code JSON data response se le raha hai, jo keh ek format hai jis se data store aur exchange kia jata hai. Code json method use kar raha hai response ko Python dictionary mein convert karne ke liye, jo keh ek data structure hai jo key-value pairs store karta hai.

Code phir dictionary mein se customers ke through loop kar raha hai aur har customer ke customer ID ko get method se le raha hai, jo keh dictionary mein diye gae key ke liye value return karta hai. Code phir /customers/{customer_id} endpoint ko request bhej raha hai, jo keh ek specific customer ke details return karta hai. Phir code status code ko check kar raha hai aur response se JSON data le raha hai. Phir code name, email, phone aur address ko dictionary se get kar raha hai, aur inhe ek naye dictionary customer_dict mein store kar raha hai. Code phir is dictionary ko ek list customer_data ke andar append kar raha hai, jo keh sari customer data ko store karega jo code API se leta hai.

Agar status code 200 nahi hai, to code print function ka use kar ke error message print kar raha hai, jo keh screen pe display karta hai. Code f prefix aur curly braces ka istemal kar raha hai, jo keh string me variable ke values ko insert karne ke liye istemal kiya jata hai.

Loop khatam hone ke baad, code write mode me customer_data.csv naam ka ek file khol raha hai, jo keh file ko create ya overwrite karne ke liye hota hai. Code open function ka istemal kar raha hai file ko kholne ke liye, aur file name, mode aur newline argument ko pass kar raha hai, jo keh specify karta hai ki file me naye line kaise likhe jayenge. Code CSV format me data ko likhne ke liye csv module ka istemal kar raha hai, jo keh comma se separate kiye gaye rows aur columns me data ko store karta hai. Code csv.writer function ka istemal kar ke writer object ko create kar raha hai, aur file object ko argument ke roop me pass kar raha hai.

Phir code file ka pehla row likh raha hai, jisme headers ya column ke naam shamil hote hai, writerow method ka istemal kar ke, aur string ka ek list argument ke roop me pass kar ke. Phir code customer data list me loop kar raha hai, aur har customer dictionary ko row ke roop me file me likh raha hai, isi method ka istemal kar ke. Code dictionary ke keys ke through uske values ko access karne ke liye square brackets ka istemal kar raha hai. Loop khatam hone ke baad, code file ko band kar raha hai, jo keh badlav ko save karta hai aur resources ko free karta hai.

=========================

- [Javascript File](index.js):

=========================

About npm:

NPM ka matlab hai "Node Package Manager". Jab hum Javascript mein koi project create karte hain, jaise React, Vue ya Angular, tou hum us project ke liye libraries aur modules install karte hain. Ye libraries aur modules pehle se hi available nahi hoti hain, humein unhe install karna hota hai. Iss mein hum NPM ka istemal karte hain.

NPM ke through hum libraries aur modules ko search, install aur manage karte hain. NPM ke database mein lakhoon se bhi zyada libraries aur modules hote hain. Inme se hum jo bhi module ya library apne project ke liye use karna chahte hain, hum usse "npm install" command se install karte hain.

"npm install" command ke through, humein wo package install karne ki zaroorat nahi hoti jaise zip file download karna aur phir manually unhe extract karna aur copy karna. NPM install karne se humare project ke dependencies ko manage karna asaan ho jata hai. Hum chahe to specific version of a package install kar sakte hain, jaise latest version, stable version ya phir koi specific version. Isse humare project ke dependencies ka version control asaan ho jata hai aur hum har waqt unhe update kar sakte hain.

How to install npm:

Agar aapko Node.js install karna hai toh sabse pehle aapko official website pe jaana hoga, jaha se aapko Node.js download karne ke liye options milenge. Aap is link pe click karke waha jaa sakte hain: https://nodejs.org/en/download/

Yaha pe aapko apne operating system ke hisaab se Node.js ka version select karna hoga. Aap Windows use karte hain toh aapko Windows Installer ko download karna hoga.

Download hone ke baad, aapko Installer pe double click karna hoga jisse installation process shuru ho jaayegi. Agar aapko installation ke dauraan koi error aata hai, toh aapko us error ko google karna hoga aur uska solution dhundhna hoga.

Agar aap already Node.js install kiye hue hain aur koi error aa raha hai, toh sabse pehle aapko apne terminal/command prompt mein "node -v" command daal ke check karna hoga ki aapke system mein Node.js installed hai ya nahi. Agar installed hai toh aap "npm cache clean --force" command daal ke apne npm cache ko clean kar sakte hain. Iske baad aap npm install karke aapko required package install karne honge.

=========================

Dependencies:

            npm install request
            npm install csv-writer
            npm install json2csv
            npm install dotenv
Inside JS:

        var request = require('request');
        var csvWriter = require('csv-writer');

Explanation:

Jab bhi hum JavaScript mein kisi aur file mein se code ko use karte hain, to hum "require" ka use karte hain. Agar humare code mein kisi aur package ka use kiya jata hai, to use install karne ke liye hum "npm" ka use karte hain. npm, Node Package Manager hai, jo ki Node.js ke saath pre-installed aata hai.

"var" ka use JavaScript mein variables declare karne ke liye kiya jata hai. Jab hum kisi package ko install karte hain aur use apne code mein "require" karte hain, to us package ke saare files humare system mein download ho jate hain. "request" package ka use hum web pages se data retrieve karne ke liye karte hain. "csv-writer" package hum tab use karte hain, jab hum CSV files mein data ko write karna chahte hain.

Jab bhi hum "require" ka use karte hain, to hume pahle se hi us package ko apne system mein install karna hota hai. "npm install" command ka use karke hum us package ko install kar sakte hain. "request" package aur "csv-writer" package ke installation ke liye "npm install request" aur "npm install csv-writer" commands ka use kiya jata hai.

Note:

Main ne neeche diye gaye dependencies istemaal karne ki koshish ki thi lekin unke kaam na karne ki wajah se main unhein kat diya hai. Main ne unhein neeche shamil kar diya hai taake tum logon ko bata sako ke tumne bhi un methods ko try kiya tha, lekin unke kaam na karne ki koi wajah thi jo mujhe pata nahi.

<s>npm install axios</s>

=========================

Details

Is code ko JavaScript mein likha gaya hai, jo ek programming language hai jiski madad se web applications, interactive websites, games aur aur bhi bahut kuch create kiya ja sakta hai. Is code mein kuch modules ya libraries ka istemal kiya gaya hai, jo extra functionality provide karte hai jaise ki request, fs aur csv-writer. Inn modules ko code ke shurwat mein require function ka istemal karke import kiya jaana chahiye.

Is code ka kaam kisi API se customer data lene ki koshish karna hai, jo ek tarika hai dusre web service ya application se communicate karne ka. API Arrivy dwara provide kiya gaya hai, jo businesses ki deliveries aur services ko manage karne mein madad karta hai. Is API ko istemal karne ke liye, is code ke paas ek API key aur auth token ki zaroorat hoti hai, jo unique identifiers hai jinki madad se API tak pahuncha jaa sakta hai. Is code mein ye values var keyword ki madad se variables mein store kiye gaye hai.

Is code mein request module ka istemal HTTP requests ko API tak bhejne ke liye kiya ja raha hai, jo internet ke zariye data bhejne aur prapt karne ke tarikay hai. Is code mein get method ka istemal data ko API se prapt karne ke liye kiya ja raha hai aur ek object ko argument ke roop mein pass kiya ja raha hai. Ye object request ke URL aur headers ko contain karta hai. URL API endpoint ka pata hai, jo ek specific function ya resource provide karta hai. Headers request ke saath bheje jaane waale additional information hai, jaise ki API key aur auth token.

Is code mein ek callback function bhi argument ke roop mein pass kiya jaata hai, jo request complete hone ke baad execute kiya jaata hai. Callback function mein teen parameters hote hai: error, response aur body. Error parameter mein request ke dauraan koi error hota hai, response parameter mein response ke baare mein information hoti hai, jaise ki status code, aur body parameter mein API dwara wapas bheja gaya data hota hai.

Is code mein if statement ka istemal karke error ko check kiya jaata hai, jo condition ke basis par decisions lene ka ek tarika hai. Agar error hai to, code console.error method ka istemal karke error ko print karta hai, jo console mein error message ke saath output display karta hai. Code function se return bhi karta hai, jo execution ko rokne ka matlab hai.

Is code mein response ke status code ko bhi check kiya jaata hai, jo number hota hai jo ye batata hai ki request successful hai ya nahi. Agar status code 200 nahi hai, jo OK ka matlab hai, to code console.error method ka istemal karke status code ko print karta hai aur function se return karta hai.

Agar koi error nahi hai aur status code 200 hai, to code response ke body ko parse karta hai, jo ek JSON string hai, aur ise JavaScript object mein convert karta hai, jo key-value pairs ko store karta hai. Is code mein JSON.parse method ka istemal kiya jaata hai.
Is code mein data object ko naye customers ke array mein map kiya gaya hai. Yeh array har customer ki sirf naam, email aur phone ko contain karega. Is code mein map method ka istemaal kia gaya hai, jo har element par aik function apply karke ek array ko transform karna ke liye istemaal kia jata hai. Yeh code ek function ko argument ke tor par pass karta hai jo customer parameter leta hai aur naam, email aur phone ki properties ke sath aik naya object return karta hai.

=========================
